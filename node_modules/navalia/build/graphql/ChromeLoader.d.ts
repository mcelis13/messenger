import { Chrome } from '../Chrome';
export interface handler {
    (chromeTab: Chrome): Promise<any>;
}
export declare class ChromeLoader {
    private actions;
    private timerId;
    private delay;
    private chrome;
    constructor({delay}?: {
        delay?: number;
    });
    run(fn: any): Promise<{}>;
    serial(): Promise<any>;
}
