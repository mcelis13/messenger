import * as chromeUtil from './chrome';
export interface options {
    maxActiveTabs?: number;
    flags?: chromeUtil.flags;
    timeout?: number;
}
export declare class ChromeHelper {
    private cdp;
    private isExpired;
    private activeTabs;
    private maxActiveTabs;
    private browserStartingPromise;
    private kill;
    private flags;
    private jobsComplete;
    private timeout;
    port: number;
    constructor(options: options);
    start(callback: any): void;
    onTabClose(targetId: string): void;
    quit(): Promise<void>;
    setExpired(): void;
    isIdle(): boolean;
    isFull(): boolean;
    getIsExpired(): boolean;
    getJobsComplete(): number;
}
